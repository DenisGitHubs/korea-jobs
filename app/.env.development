# Dev defaults, loaded automatically by `npm run dev` (Vite mode=development).
# COMMITTED ON PURPOSE — contains only VITE_* switches, NO secrets.
# Lets the app open in a plain browser on mock data, before any backend exists.

VITE_API_MODE=mock
VITE_API_BASE_URL=
VITE_TEST_INITDATA=
